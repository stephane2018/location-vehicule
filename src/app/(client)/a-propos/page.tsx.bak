import React from "react";
import Link from "next/link";
import { Button } from "@/shared/components/ui/button";
import { Card, CardContent } from "@/shared/components/ui/card";
import { Separator } from "@/shared/components/ui/separator";
import {
  Award,
  Building2,
  Car,
  Clock,
  Heart,
  MapPin,
  Shield,
  Star,
  Target,
  Users,
} from "lucide-react";

// ─── Constants ───────────────────────────────────────────────────────────────

const STATS = [
  { value: "200+", label: "Véhicules", icon: Car },
  { value: "5", label: "Agences", icon: Building2 },
  { value: "5 000+", label: "Clients satisfaits", icon: Users },
  { value: "98%", label: "Satisfaction", icon: Star },
];

const VALUES = [
  {
    icon: Shield,
    title: "Fiabilité",
    description:
      "Des véhicules récents et bien entretenus pour garantir votre sécurité sur les routes de Côte d'Ivoire.",
  },
  {
    icon: Heart,
    title: "Service client",
    description:
      "Une équipe dédiée disponible 6j/7 pour vous accompagner avant, pendant et après votre location.",
  },
  {
    icon: Target,
    title: "Transparence",
    description:
      "Des tarifs clairs et sans frais cachés. Assurance tous risques incluse dans chaque location.",
  },
  {
    icon: Clock,
    title: "Simplicité",
    description:
      "Réservez en quelques clics et payez par Mobile Money. Pas de démarches compliquées.",
  },
];

const TEAM = [
  {
    name: "Kouadio Aimé",
    role: "Directeur Général",
    description: "15 ans d'expérience dans le transport et la location automobile en Afrique de l'Ouest.",
  },
  {
    name: "Traoré Aminata",
    role: "Directrice des Opérations",
    description: "Responsable de la qualité de service et de la gestion de la flotte sur l'ensemble du réseau.",
  },
  {
    name: "Koné Mamadou",
    role: "Responsable Commercial",
    description: "En charge du développement commercial et des partenariats entreprises.",
  },
];

const MILESTONES = [
  { year: "2018", event: "Création d'AutoLoc CI à Abidjan avec une flotte de 10 véhicules" },
  { year: "2019", event: "Ouverture de la deuxième agence au Plateau" },
  { year: "2020", event: "Lancement du paiement Mobile Money et expansion à 50 véhicules" },
  { year: "2022", event: "Ouverture des agences de Yamoussoukro et Bouaké" },
  { year: "2024", event: "Ouverture de l'agence de San Pedro et lancement de la gamme Luxe" },
  { year: "2026", event: "Plus de 200 véhicules et 5 000 clients satisfaits" },
];

// ─── Main Page ───────────────────────────────────────────────────────────────

export default function AProposPage() {
  return (
    <div className="min-h-screen bg-background">
      <div className="h-1 w-full bg-primary" aria-hidden="true" />

      <main className="mx-auto max-w-screen-xl px-4 py-8 sm:px-6 lg:px-8">
        {/* Hero */}
        <div className="text-center max-w-3xl mx-auto mb-12">
          <h1 className="text-3xl font-bold text-foreground sm:text-4xl">
            À propos d&apos;AutoLoc CI
          </h1>
          <p className="mt-4 text-lg text-muted-foreground leading-relaxed">
            Depuis 2018, AutoLoc CI est la référence de la location de
            véhicules en Côte d&apos;Ivoire. Notre mission : rendre la
            mobilité accessible, fiable et abordable pour tous.
          </p>
        </div>

        {/* Stats */}
        <div className="grid grid-cols-2 gap-4 sm:grid-cols-4 mb-16">
          {STATS.map((stat) => (
            <div
              key={stat.label}
              className="flex flex-col items-center gap-2 rounded-xl border bg-card p-6 shadow-sm text-center"
            >
              {React.createElement(stat.icon, { className: "size-6 text-primary" })}
              <p className="text-3xl font-bold text-foreground">{stat.value}</p>
              <p className="text-sm text-muted-foreground">{stat.label}</p>
            </div>
          ))}
        </div>

        {/* Our Story */}
        <section className="mb-16">
          <div className="grid grid-cols-1 gap-8 lg:grid-cols-2 items-center">
            <div>
              <h2 className="text-2xl font-bold text-foreground mb-4">
                Notre histoire
              </h2>
              <div className="space-y-4 text-sm text-muted-foreground leading-relaxed">
                <p>
                  AutoLoc CI a été fondé en 2018 à Abidjan avec une vision
                  simple : offrir un service de location de véhicules de
                  qualité, adapté aux réalités ivoiriennes. Nous avons
                  commencé avec 10 véhicules et une seule agence à Cocody.
                </p>
                <p>
                  Aujourd&apos;hui, nous sommes fiers de disposer d&apos;une
                  flotte de plus de 200 véhicules répartis dans 5 agences à
                  travers le pays. Notre gamme va de la citadine économique
                  au SUV de luxe, pour répondre à tous les besoins.
                </p>
                <p>
                  Notre engagement : des véhicules récents, un service
                  irréprochable et des tarifs justes. Nous innovons
                  constamment avec le paiement Mobile Money et une
                  plateforme de réservation en ligne simple et rapide.
                </p>
              </div>
            </div>
            {/* Image placeholder */}
            <div className="rounded-2xl bg-gradient-to-br from-primary/20 to-primary/5 h-64 lg:h-80 flex items-center justify-center border">
              <div className="text-center space-y-2">
                <Building2 className="size-16 text-primary/30 mx-auto" />
                <p className="text-sm text-muted-foreground">Siège AutoLoc CI — Abidjan</p>
              </div>
            </div>
          </div>
        </section>

        {/* Timeline */}
        <section className="mb-16">
          <h2 className="text-2xl font-bold text-foreground text-center mb-8">
            Notre parcours
          </h2>
          <div className="max-w-2xl mx-auto space-y-0">
            {MILESTONES.map((milestone, i) => (
              <div key={milestone.year} className="flex gap-4">
                <div className="flex flex-col items-center">
                  <div className="flex size-10 shrink-0 items-center justify-center rounded-full bg-primary text-primary-foreground text-xs font-bold">
                    {milestone.year}
                  </div>
                  {i < MILESTONES.length - 1 && (
                    <div className="w-px flex-1 bg-border my-1" />
                  )}
                </div>
                <div className="pb-8">
                  <p className="text-sm text-foreground font-medium pt-2">
                    {milestone.event}
                  </p>
                </div>
              </div>
            ))}
          </div>
        </section>

        {/* Values */}
        <section className="mb-16">
          <h2 className="text-2xl font-bold text-foreground text-center mb-8">
            Nos valeurs
          </h2>
          <div className="grid grid-cols-1 gap-5 sm:grid-cols-2 lg:grid-cols-4">
            {VALUES.map((value) => (
              <Card key={value.title} className="gap-0 py-0 text-center">
                <CardContent className="p-6 space-y-3">
                  <div className="flex size-12 items-center justify-center rounded-xl bg-primary/10 mx-auto">
                    {React.createElement(value.icon, { className: "size-6 text-primary" })}
                  </div>
                  <h3 className="text-base font-semibold text-foreground">
                    {value.title}
                  </h3>
                  <p className="text-sm text-muted-foreground leading-relaxed">
                    {value.description}
                  </p>
                </CardContent>
              </Card>
            ))}
          </div>
        </section>

        {/* Team */}
        <section className="mb-16">
          <h2 className="text-2xl font-bold text-foreground text-center mb-8">
            Notre équipe dirigeante
          </h2>
          <div className="grid grid-cols-1 gap-5 sm:grid-cols-3 max-w-3xl mx-auto">
            {TEAM.map((member) => (
              <Card key={member.name} className="gap-0 py-0 text-center">
                <CardContent className="p-6 space-y-3">
                  <div className="flex size-16 items-center justify-center rounded-full bg-primary/10 mx-auto">
                    <Users className="size-7 text-primary" />
                  </div>
                  <div>
                    <h3 className="text-base font-semibold text-foreground">
                      {member.name}
                    </h3>
                    <p className="text-xs text-primary font-medium">
                      {member.role}
                    </p>
                  </div>
                  <p className="text-sm text-muted-foreground leading-relaxed">
                    {member.description}
                  </p>
                </CardContent>
              </Card>
            ))}
          </div>
        </section>

        <Separator className="mb-16" />

        {/* CTA */}
        <div className="text-center mb-8">
          <h2 className="text-2xl font-bold text-foreground">
            Prêt à rouler avec AutoLoc CI ?
          </h2>
          <p className="mt-2 text-muted-foreground max-w-md mx-auto">
            Rejoignez les milliers de clients qui nous font confiance pour
            leurs déplacements en Côte d&apos;Ivoire.
          </p>
          <div className="mt-6 flex flex-col sm:flex-row gap-3 justify-center">
            <Button size="lg" className="font-semibold" asChild>
              <Link href="/vehicules">Voir nos véhicules</Link>
            </Button>
            <Button variant="outline" size="lg" asChild>
              <Link href="/contact">Nous contacter</Link>
            </Button>
          </div>
        </div>
      </main>
    </div>
  );
}
